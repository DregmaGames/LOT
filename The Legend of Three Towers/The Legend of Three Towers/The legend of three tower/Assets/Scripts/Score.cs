using UnityEngine;
using System.Collections;

public class Score : MonoBehaviour {
/*
	public int score;
	private LevelIndexer indexer;
	public TextMesh scoreTxt;


	void Start () {
		indexer = GameObject.FindWithTag("LevelIndexer").GetComponent<LevelIndexer>();
		if(ES2.Exists("Score_" + indexer.era + "-" + indexer.level))
			score = ES2.Load<int>("Score_" + indexer.era + "-" + indexer.level);
		else
			score = 0;
	}


	void Update(){
		scoreTxt.text = score.ToString("0000");
	}
	*/
}